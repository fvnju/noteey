{
  "name": "@noteey/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "dev": "doppler run -- convex dev",
    "dev:setup": "doppler run -- convex dev --configure --until-success",
    "deploy": "doppler run -- convex deploy"
  },
  "dependencies": {
    "convex": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@noteey/config": "workspace:*",
    "@types/node": "^24.3.0",
    "typescript": "catalog:"
  }
}
